# A3.1.4 visible original interactive M00 candidate

Status: **FIRST PHYSICALLY ACCEPTED VISIBLE ORIGINAL INTERACTIVE M00 BASELINE**.

Physical outcome: visible original M00 geometry, original player/session/camera
path, 1,226 stable interactive frames, zero rejected/unsupported submissions,
and clean START exit: PASS. Analog camera usability: FAIL because the Vita
device boundary supplied ±32,767 to a Renegade ±1,000 logical-axis contract.
Textures: NOT IMPLEMENTED / NOT DISPLAYED because the Vita `TextureClass` path
explicitly marked resources initialized without decode, native upload, or bind.
HUD Font3D and audio remain deferred; campaign and multiplayer are not accepted.
The raw returned log is preserved unchanged in `../../baselines/A3.1.4/`.

## Frozen predecessor

A3.1.3 is the first physically accepted original interactive-lifecycle
baseline: 1,354 stable interactive frames, original local transport, M00,
player/cGod/Commando, input/update/render calls, START exit, and teardown all
passed. It is not visible interactive gameplay: its render counters stayed at
zero. Its returned stale-named raw log is immutable at
`../../baselines/A3.1.3/a31.1-runtime.log`.

## A3.1.4 repair and host proof

The A3.1.3 Vita adapter called `CombatManager::Render()` directly. Original
Commando `GameModeManager::Render()` encloses that call with the active
`PhysicsSceneClass::Pre_Render_Processing(*COMBAT_CAMERA)` and
`Post_Render_Processing()` calls. The pre-render operation fills the visible
static and dynamic lists consumed by `PhysicsSceneClass::Customized_Render`.

The shared host/Vita A3.1.4 orchestration restores precisely that original
scene/camera envelope. It preserves original Combat, Commando, cGod, camera,
PhysicsScene, render traversal, and Vita backend. The host ASan validation ran
two clean in-process lifecycle cycles with 120 complete original frames each.
Its first interactive frame submitted 205 meshes, 12,426 vertices, and 8,661
triangles; rejected and unsupported submissions were both zero.

Targeted UBSan additionally exposed and closed an unrelated original M00
Encyclopedia SaveLoad zero-length chunk issue before this candidate was
revalidated: the loader no longer forms `&bit_array[0]` unless it reads bytes.
The same two-cycle 120-frame visible path now passes that targeted UBSan
configuration as well as ASan.

Font3D HUD, audio output, and procedural transition-material passes remain
explicitly deferred platform capabilities. They do not suppress base M00 scene
traversal.

## Candidate

- VPK artifact: `<managed-dist>/RenegadeVita-A3.1.4.vpk`
- SHA-256: `495c5c0954c0c2d92d04690ba53442c87277363e2ea7f92f2f394b4ac66afd2b`
- Runtime log: `ux0:data/renegade/user/logs/a31.4-runtime.log`
- VPK contents: `eboot.bin` and `sce_sys/param.sfo` only; no retail data.

Install this VPK manually with VitaShell. Do not recopy the already-installed
retail data. Confirm the A3.0 overview, press START once to enter A3.1.4, then
confirm a visible original player-owned M00 view rather than a black screen.
Exercise movement/look, leave it running through the 120-frame checkpoint, and
press START a second time for clean exit. Return the new runtime log and any
`psp2core-*.psp2dmp` if a crash occurs.

Expected log first line: `Renegade Vita A3.1.4 visible interactive candidate`.
Expected durable breadcrumbs include A3.0 completion, interactive render state,
nonzero first-frame geometry, 120-frame checkpoint, START detection, Combat and
network shutdown, renderer shutdown, audio teardown, and clean process exit.
