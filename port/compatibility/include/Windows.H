#pragma once

// Original Westwood sources use this mixed-case spelling on the
// case-insensitive Win32 filesystem. Keep the alias centralized at the
// platform boundary rather than editing source-owned include directives.
#include "windows.h"
