#pragma once

// Include-overlay for the original Commando source.  The old GameSpy admin
// surface is a compatibility facade over the native provider boundary.
#include "renegade_network_provider.h"
