#pragma once

// Original Combat/Commando sources spell this legacy Win32 header in upper
// case.  Keep case-sensitive POSIX builds at the same centralized platform
// boundary as the corresponding SYSTEMTIME/GetSystemTime contract.
#include "windows.h"
